# Purpose: Local Prometheus scrape configuration for docker-compose development.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "llm-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["llm-api:8000"]
